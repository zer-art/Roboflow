# Simulation Configs

Store configuration files here to control simulation parameters without changing code.
- `simulation.yaml`: Main simulation settings (runs, random seed, etc).
- `agent.yaml`: RL agent hyperparameters.
